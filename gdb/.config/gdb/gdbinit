python
import os
import subprocess
from pathlib import Path

try:
    root = Path(subprocess.check_output(
        ["yamroot"],
        text=True,
        stderr=subprocess.DEVNULL,
    ).strip())

    if root.is_dir():
        gdb.execute(f"set substitute-path /KARANA_SBOX_ROOT {root}")
        gdb.execute(f"set filename-display absolute")
except Exception:
    pass
end
